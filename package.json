{
  "name": "meme-gtd",
  "version": "0.1.0",
  "private": true,
  "description": "CLI toolkit for meme-gtd (init + memo commands)",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r --filter meme-gtd-cli build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "dev": "pnpm --filter meme-gtd-cli dev",
    "format": "pnpm -r format"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
