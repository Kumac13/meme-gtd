{
  "name": "meme-gtd-logger",
  "version": "0.47.3",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm run clean && tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "test": "pnpm run -s build && node --test test/*.test.js"
  },
  "dependencies": {
    "meme-gtd-config": "workspace:*",
    "pino": "^9.2.0",
    "pino-pretty": "^11.2.0",
    "pino-roll": "^2.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "rimraf": "^5.0.7",
    "typescript": "^5.5.4"
  }
}
